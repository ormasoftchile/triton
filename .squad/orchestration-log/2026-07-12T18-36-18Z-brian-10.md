# Orchestration Log: brian-10

**Agent:** Brian (sonnet-4.6)  
**Task:** Add distinct split/side glyph for openPreviewToSide  
**Timestamp:** 2026-07-12T18:36:18Z  
**Status:** COMPLETE

## Outcome

Made `triton.openPreviewToSide` button visually distinct from `triton.openPreview` by adding dedicated side-pane glyph variants.

**Rationale:** Both buttons previously showed identical single-pane glyphs. Following VS Code markdown extension pattern, side-preview command now gets split/two-pane glyph (rounded rect + vertical divider + checkmark in right pane).

**Files created:**
- `extension/resources/preview-side-light.svg` (stroke `#424242`)
- `extension/resources/preview-side-dark.svg` (stroke `#C5C5C5`)

Updated `triton.openPreviewToSide` command block in `extension/package.json`. `triton.openPreview` remains unchanged.

**Decision recorded in:** `.squad/decisions.md`
