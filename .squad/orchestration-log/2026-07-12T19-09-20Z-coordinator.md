# Orchestration: External Theming Decisions (Coordinator)

**Timestamp:** 2026-07-12T19:09:20Z  
**Agent:** Coordinator (via Cristian's authority)  
**Task:** Resolve 5 open questions from Leslie's external theming design plan

## Scope

Cristian's decision calls on Leslie's 5 open questions regarding external theme implementation:

### Decisions Made

1. **Unknown-key policy = STRICT / ERROR** (overrides Leslie's draft warning recommendation)
2. **Name derivation = from filename when `name` omitted**
3. **`base` key = supported, optional, defaults to `"default"`**
4. **File extension = `.triton-theme.json`**
5. **Phase 0 cache-key mechanism = shell `printf >> tempfile`**

## Output

**Document:** `.squad/decisions/inbox/coordinator-external-theme-decisions.md` (1,522 bytes)
- Recorded 5 decisions with rationales
- Linked to Leslie's Phase §10 plan
- Marked plan as APPROVED for implementation

## Status

Archived to `.squad/decisions.md` after merging with Leslie's design. Inbox file deleted post-merge.

## Impact

Plan now cleared for Phase 0 start (cache-key fix in triton.sty). Unknown-key policy override changes Phase 1 validation logic: must reject (not warn) on unknown keys.
