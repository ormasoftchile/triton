# Orchestration Log: brian-9

**Agent:** Brian (sonnet-4.6)  
**Task:** Fix preview toolbar buttons invisible in dark mode  
**Timestamp:** 2026-07-12T18:36:18Z  
**Status:** COMPLETE

## Outcome

Fixed `triton.openPreview` and `triton.openPreviewToSide` toolbar button visibility on dark themes.

**Root cause:** SVG used `stroke="currentColor"`, which VS Code resolves to black on toolbar icons (black is invisible on dark background).

**Fix:** Created color-baked SVG variants:
- `extension/resources/preview-light.svg` (ink `#424242`)
- `extension/resources/preview-dark.svg` (ink `#C5C5C5`)

Updated both command blocks in `extension/package.json` to reference light/dark variants.

**Build:** `pnpm build:extension` passed (esbuild, 289ms).

**Decision recorded in:** `.squad/decisions.md`
