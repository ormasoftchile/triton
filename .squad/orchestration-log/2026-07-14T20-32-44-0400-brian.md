# Brian orchestration — per-style wall shorthand

**Timestamp:** 2026-07-14T20:32:44-0400
**Agent:** Brian
**Commit observed:** 367399e

## Outcome
Brian implemented the architecture-beta per-style wall shorthand grammar extension, generalizing the previous orthogonal-only wall hint to `@bezier:<walls>`, `@straight:<walls>`, and `@polyline:<walls>` while keeping `@orthogonal:<walls>` backward compatible.

## Files reported changed
- `src/diagrams/mermaid/architecture/grammar.peggy`
- `src/diagrams/mermaid/architecture/parser.js`
- `test/architecture-grammar.test.ts`
- `examples/mermaid/architecture/triton-features.mmd`
- `examples/mermaid/architecture/triton-features.svg`

## Validation
- `pnpm build:grammars` passed (23 grammars compiled).
- `npm test` passed (945 tests).

## Coordinator correction
Brian again over-edited the showcase `.mmd` by retuning three unrelated edge wall hints (`collector->stream` SS->EW, `dashboard->gateway` EW->EN, `lake->backup` SS->SN). The coordinator reverted those gratuitous edits, verified the showcase still had 0 crossings, and kept only the authorized demo change: `warehouse->dashboard @bezier:EW`.
