{
  "name": "@timeline-compiler/cli",
  "version": "0.1.0",
  "description": "Timeline Compiler CLI — render, validate, and inspect timeline IR files",
  "type": "module",
  "bin": {
    "timeline": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "eslint src test",
    "test": "vitest run"
  },
  "dependencies": {
    "@timeline-compiler/core": "workspace:*",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
