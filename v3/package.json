{
  "name": "@triton/core-v3",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Triton v3 — contracts-first unified diagram compiler",
  "scripts": {
    "build:grammars": "node scripts/build-grammars.mjs",
    "build": "pnpm build:grammars && tsc --project tsconfig.json",
    "preview": "node scripts/preview.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "peggy": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.2.0",
    "@types/node": "^22.0.0"
  }
}
