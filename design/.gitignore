# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.out
*.toc
*.log
*.xdv
*.fls
*.fdb_latexmk
*.synctex.gz

# Triton inline-render outputs (regenerated at compile time)
*.triton-cache/
*.triton-src.triton

# Package source copied in from ../latex at build time (see Makefile).
# Not committed: a symlink breaks on Windows, so we copy instead.
triton.sty
