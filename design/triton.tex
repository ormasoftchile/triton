\documentclass{MastersThesis}

% ── Packages ────────────────────────────────────────────────────────────────
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{newunicodechar}
\newunicodechar{—}{\textemdash}
\newunicodechar{–}{\textendash}
\newunicodechar{§}{\S}
\newunicodechar{→}{$\rightarrow$}
\newunicodechar{✓}{\checkmark}
\newunicodechar{✗}{$\times$}
\usepackage{listings}
\usepackage{inconsolata}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{caption}
\usepackage{fancyvrb}
\usepackage{hyperref}
\hypersetup{hidelinks}

% ── Listings ────────────────────────────────────────────────────────────────
\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  breakatwhitespace=true,
  frame=single,
  framerule=0.5pt,
  rulecolor=\color{gray!50},
  backgroundcolor=\color{gray!5},
  numbers=left,
  numberstyle=\tiny\color{gray},
  numbersep=8pt,
  tabsize=2,
  showstringspaces=false,
  captionpos=b
}

% Triton DSL (.mmd) — diagram source language
\lstdefinelanguage{triton}{
  morekeywords={flowchart,sequenceDiagram,timeline,tree,plan,avl,rbtree,btree,
    radix,segtree,heap,array,linkedlist,memory,page,topology,poster,cell,link,
    title,columns,insert,order,over,reduce,index,ptr,cells,region,var,object,
    node,group,costs,tier,slots,tuples,LR,TB,TD},
  sensitive=false,
  comment=[l]{\%\%},
  morestring=[b]"
}

% TypeScript-ish
\lstdefinelanguage{ts}{
  morekeywords={const,let,interface,type,export,import,from,async,await,return,
    readonly,extends,implements,function,class,new,this,void,Promise,string,
    number,boolean},
  sensitive=true,
  comment=[l]{//},
  morecomment=[s]{/*}{*/},
  morestring=[b]',
  morestring=[b]"
}

% ── Metadata ────────────────────────────────────────────────────────────────

% Figure rendered by Triton itself from an example source.
%   \ourfig[<width>]{<figure-name>}{<caption>}
\newcommand{\ourfig}[3][\linewidth]{%
  \begin{figure}[htbp]
    \centering
    \includegraphics[width=#1]{figures/#2.png}
    \caption{#3}
  \end{figure}%
}

\title{Triton\\[0.4em]\Large A Contracts-First, Deterministic\\
  Mermaid-Superset Diagram Compiler\\[0.3em]\normalsize Architecture \& Specification}
\author{Triton Project}
\date{June 2026}

% ── Document ────────────────────────────────────────────────────────────────
\begin{document}
\maketitle

\begin{quote}\small
\textbf{Scope of this document.} This specification describes Triton \emph{as it
is implemented}. Every construct, contract, and example here corresponds to
shipped code under \texttt{src/} and runnable sources under \texttt{examples/}
(each \texttt{.mmd} file beside its rendered \texttt{.svg}). It is deliberately
short: where the implementation is the source of truth, the document points at
it rather than restating it. Features that are not built are listed explicitly in
\S\ref{sec:status}, not described as if they were. \textbf{The figures are
dogfooded:} every diagram below is rendered by Triton itself from an example
source (regenerate with \texttt{pnpm figures}).
\end{quote}

\tableofcontents
\newpage

\input{sections/01-overview}
\input{sections/02-architecture}
\input{sections/03-scene}
\input{sections/04-kernels}
\input{sections/05-families}
\input{sections/06-composition}
\input{sections/07-themes-determinism}
\input{sections/08-status}
\input{sections/09-latex-integration}

\end{document}
